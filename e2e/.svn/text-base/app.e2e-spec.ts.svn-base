import { AggregatorPage } from './app.po';

describe('aggregator App', function() {
  let page: AggregatorPage;

  beforeEach(() => {
    page = new AggregatorPage();
  });

  it('should display message saying app works', () => {
    page.navigateTo();
    expect(page.getParagraphText()).toEqual('app works!');
  });
});
